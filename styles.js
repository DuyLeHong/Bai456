import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
    },
    bigText : {
        fontSize: 40,
        textAlign: 'center',
    },
    item: {
        padding: 10,
        fontSize: 18,
        height: 44,
    },
});

export default styles